# Freight Rate Prediction Challenge

## Project Overview
This repository contains a machine learning pipeline designed to predict freight rates based on shipment attributes, market dynamics, and spatial-temporal features.

## Setup & Environment
Ensure you have Python installed, then install the required dependencies:

```bash
pip install -r requirements.txt